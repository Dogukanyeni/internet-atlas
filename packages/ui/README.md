# @atlas/ui

Shared React components. Empty until Phase 14, when the Atlas map arrives and the first
components are actually shared between pages.

Rule: a component moves here only when it is used in two places. Early sharing creates
the wrong abstraction.
