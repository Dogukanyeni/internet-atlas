# @atlas/api-client

**Generated code. Do not edit by hand.**

`openapi.json` is exported from the FastAPI application. The TypeScript client is
generated from it in Phase 6, when the first real endpoints exist.

Regenerate:

```bash
make openapi
```

CI fails if the committed schema does not match the code (ADR-011).
